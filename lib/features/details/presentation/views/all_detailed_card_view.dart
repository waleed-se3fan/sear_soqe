import 'package:flutter/material.dart';

class AllDetailedCard extends StatelessWidget {
  const AllDetailedCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}