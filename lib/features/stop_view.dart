import 'package:flutter/material.dart';

class StopView extends StatelessWidget {
  const StopView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('البرنامج موقوق لان المبرمج لم ياخذ حقوقه')),
    );
  }
}
